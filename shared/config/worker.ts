/**
 * Конфигурация системы расчета зарплаты мойщиков
 */

export const WORKER_CONFIG = {
  /**
   * Базовая ставка за выход (один раз в день)
   * Выплачивается каждому мойщику в начале рабочего дня
   */
  BASE_SALARY: 500,

  /**
   * Базовая ставка при работе в паре
   * Выплачивается каждому мойщику в начале рабочего дня
   */
  BASE_SALARY_PAIR: 250,

  /**
   * Процент от стоимости заказа, который получает мойщик
   * 0.4 = 40%
   */
  PERCENTAGE: 0.4,

  /**
   * Процент от стоимости заказа при работе в паре
   * 0.2 = 20% каждому
   */
  PERCENTAGE_PAIR: 0.2,
} as const;

/**
 * Конфигурация системы расчета зарплаты шиномонтажников
 */
export const TIRE_TECHNICIAN_CONFIG = {
  /**
   * Процент от стоимости заказа, который получает шиномонтажник
   * 0.5 = 50%
   */
  PERCENTAGE: 0.5,
  /**
   * Фиксированная плата за услугу хранения резины
   */
  STORAGE_FEE: 300,
  /**
   * Список названий услуг хранения резины
   * Используется для определения, когда применять фиксированную плату вместо процента
   */
  STORAGE_SERVICE_NAMES: [
    'Хранение резины',
    'Сезонное хранение резины'
  ] as const,
} as const;
